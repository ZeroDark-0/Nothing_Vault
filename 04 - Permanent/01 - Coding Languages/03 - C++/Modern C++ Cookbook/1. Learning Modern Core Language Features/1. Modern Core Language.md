---
date: 2026-06-24T16:21
tags: []
---

-> abbreviated function

-> auto introduced in c++11 and c++14
    (AAA) almost always auto

-> auto {`int, double, char, const*, std::initializer_list<int>`}

-> auto name = type_id {expression}

-> auto support lambda
    `auto upper = [](char const c){return toupper(c);};` and the parameter

-> use template with no specific type.

-> the good practice of (OOP) have the good level of/easy to mock object(lesser the type, generic the code -> this is why it is recommended to use `auto`)

->
```cpp
std::unordered_map<std::string,int> freq; 
for(const auto& word : words){
    freq[word]++;
}
```

-> clang and GCC (but not MSVC)